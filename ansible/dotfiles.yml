---
- name: dotfiles
  hosts: localhost
  roles:
    - {role: zsh, tags: ['zsh']}
    - {role: git, tags: ['git']}
    - {role: vim, tags: ['vim']}
    - {role: tmux, tags: ['tmux']}
    - {role: alacritty, tags: ['alacritty']}
    - {role: cli, tags: ['cli']}
    - {role: gui, tags: ['gui']}
    - {role: font, tags: ['font']}
