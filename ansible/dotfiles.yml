---
- name: dotfiles
  hosts: localhost
  roles:
    - {role: zsh, tags: ['zsh']}
    - {role: git, tags: ['git']}
    - {role: vim, tags: ['vim']}
    - {role: tmux, tags: ['tmux']}
  # TODO: vscode, alacritty
